server:
  port: 8090

spring:
  application:
    name: mueblesstgopep1
  jpa:
    show-sql: true
    hibernate:
      dialect: MySQL8Dialect
      ddl-auto: update
  datasource:
    #url: jdbc:mysql://localhost:3306/tingesodb
    #username: root
    #password: password
    url: ${DB_URL}
    dbname: ${DB_NAME}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: ${DB_DTSRC}